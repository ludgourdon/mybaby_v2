{% extends "base.html.twig" %}
{% block content %}
    <div class="row" id="app">
        <div class="col-sm">
            <h2>Naissance</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}


